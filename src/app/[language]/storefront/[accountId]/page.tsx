import { ProductGrid } from '@/components/products/ProductGrid';

const StorefrontPage = () => {
  return (
    <div>
      <ProductGrid titleTranslationKey='storefront.products.title' />
    </div>
  );
};

export default StorefrontPage;
