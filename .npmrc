# Authenticates npm's git clone for github: shorthand packages on Vercel.
# Set GITHUB_TOKEN in Vercel environment variables with repo read access to stripe-demos org.
//github.com/:_authToken=${GITHUB_TOKEN}
